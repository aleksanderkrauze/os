[package]
name = "os"
description = "A simple OS following blog series: https://os.phil-opp.com/."
version = "0.1.0"
edition = "2021"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"